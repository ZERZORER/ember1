<form {{action 'save' on="submit" }}>
	<div class="form-group">
		<label>Name</label>
			{{input type="text" value=model.name class="form-control" maxlength=20}}
	</div>
	<div>
    	<button type="submit" class="btn btn-default">Save</button>
  	</div>	
</form>
	