
import { productPropertyType } from 'ember1/helpers/product-property-type';
import { module, test } from 'qunit';

module('Unit | Helper | product property type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productPropertyType([42]);
  assert.ok(result);
});

